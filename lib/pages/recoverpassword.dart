import 'package:flutter/material.dart';

class RecoveredPassword extends StatelessWidget {
  RecoveredPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
